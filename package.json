{
    "name": "gerenciador-financeiro-root",
    "private": true,
    "version": "1.0.0",
    "engines": {
        "node": "20.x"
    },
    "scripts": {
        "build": "cd web && npm install && npm run build",
        "install": "cd web && npm install && cd ../server && npm install"
    }
}